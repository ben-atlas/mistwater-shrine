export default function(THREE){const g=new THREE.Group();const green=new THREE.MeshStandardMaterial({color:0x5f9b4c,roughness:.5,metalness:.02,side:THREE.DoubleSide});green.name='foliage';const dark=new THREE.MeshStandardMaterial({color:0x245c38,roughness:.65,side:THREE.DoubleSide});dark.name='foliage';const shape=new THREE.Shape();const n=48;for(let i=0;i<=n;i++){const a=i/n*Math.PI*2+.2,r=1.35*(1-.11*Math.exp(-Math.pow((a%(Math.PI*2)-.2)/.22,2)));const x=Math.cos(a)*r,z=Math.sin(a)*r*(.88+.04*Math.sin(a*5));i?shape.lineTo(x,z):shape.moveTo(x,z)}const geo=new THREE.ShapeGeometry(shape,12);geo.rotateX(-Math.PI/2);const leaf=new THREE.Mesh(geo,green);leaf.position.y=.1;leaf.castShadow=leaf.receiveShadow=true;g.add(leaf);const rim=new THREE.Mesh(new THREE.TorusGeometry(1.25,.045,6,48),dark);rim.rotation.x=Math.PI/2;rim.scale.z=.88;rim.position.y=.08;g.add(rim);for(let i=0;i<14;i++){const a=i/14*Math.PI*2;const rib=new THREE.Mesh(new THREE.BoxGeometry(.025,.018,1.08),dark);rib.position.set(Math.sin(a)*.52,.125,Math.cos(a)*.52);rib.rotation.y=a;g.add(rib)}const stem=new THREE.Mesh(new THREE.CylinderGeometry(.055,.075,.34,8),dark);stem.position.y=.17;g.add(stem);g.userData.expectedSize={width:2.7,height:.34,depth:2.4};return g}
